{
  "compilerOptions": {
    "target": "ESNext",
    "useDefineForClassFields": true,
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "Node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,

    /* ----- 여기가 핵심입니다 ----- */
    // JSX 코드를 어떻게 컴파일할지 설정합니다.
    // 'react-jsx'는 React 17 이상에서 사용하는 새로운 JSX 변환 방식입니다.
    "jsx": "react-jsx",

    // Vite의 import.meta.env 타입을 인식하도록 설정합니다.
    "types": ["vite/client"] 
  },
  "include": ["src"], // 타입스크립트 컴파일에 포함할 파일들의 경로
  "references": [{ "path": "./tsconfig.node.json" }] // vite.config.ts 등을 위한 별도 설정 참조
}
